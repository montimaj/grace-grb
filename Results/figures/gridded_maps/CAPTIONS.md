# Figure captions — gridded maps

Generated by `figure_captions.write_index`. Edit the caption in the script that draws the figure, not here — this file is overwritten on every run.

Each caption states what the panel does *not* establish as well as what it shows; carry them into the manuscript text rather than re-deriving them.

**`Fig_monthly_climatology_0p1deg.png`**

2000-01 to 2025-12. Anomalies relative to the 2004.0–2010.0 GRACE baseline.

**`Fig_seasonal_mean_0p1deg.png`**

2000-01 to 2025-12.

**`Fig_trend_0p1deg.png`**

Theil-Sen slope per pixel. Dots mark pixels whose Mann-Kendall trend does NOT survive Benjamini-Hochberg FDR at 0.05 (1467 of 9538); the series is deseasonalised and Var(S) is inflated for serial dependence (Hamed-Rao), without which a persistent monthly series returns significance almost everywhere. The test is still liberal under strong persistence, and the pixels are not independent: the trend is inherited from GRACE by mass conservation, so its large-scale pattern is observed while its within-mascon detail is interpolated.

**`Fig_rms_0p1deg.png`**

RMS of the monthly anomaly at each pixel over 2000-01-2025-12. This is the amplitude of the reconstructed signal, NOT a per-pixel error: scoring error per pixel would require a per-pixel observation, which GRACE does not provide.

**`Fig_method_comparison_2015-06.png`**

All three reproduce GRACE at mascon scale; they differ only in the sub-mascon structure they imply.

**`Fig_mascon_skill_xgboost.png`**

Each patch is one 3 deg GRACE mascon, coloured by the R² the model achieved there when that mascon (and its neighbours) were withheld from training. Skill is shown at mascon scale because that is the scale at which the target is independent; a per-pixel version would imply evidence that ~20 independent observations cannot support.

**`Fig_uncertainty_components_0p1deg.png`**

Within-mascon spread is a LOWER BOUND: GRACE resolves ~20 mascons here, so no observation constrains sub-mascon structure.
